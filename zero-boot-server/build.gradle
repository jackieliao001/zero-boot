jar.enabled = true
bootJar.enabled(true)
dependencies {
    implementation project(':zero-boot-system')
    implementation project(':zero-boot-module:zero-module-monitor')
//    implementation(project(':zero-boot-module:zero-module-generator'))
}
